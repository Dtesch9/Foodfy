{% extends "home/layout.njk" %}

{% block title %} <title>Você buscou por: {{filter}}</title> {% endblock %}

{% block content %}

  <div class="app">
    <section>
      <h2>Mais acessadas</h2>
      <div class="cards">
      {% for item in items %}
        <div class="card">
          <img src="{{item.image}}" alt="">
          <h6>{{item.title}}</h6>
          <p>{{item.author}}</p>
        </div>
      {% endfor %}
    </section>
  </div>

{% endblock %}